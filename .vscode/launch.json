{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug orrp",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/orrp",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "lldb",
      "preLaunchTask": "build-orrp",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for lldb",
          "text": "settings set target.inline-breakpoint-strategy always"
        }
      ]
    }
  ]
}